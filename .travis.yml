language: python

matrix:
  include:
    - os: linux
      python: "3.6.1"
    - os: osx
      python: "3.6.1"
      language: generic

install: pip install -q -r requirements.txt -r requirements-tests.txt

script: ./test.sh
